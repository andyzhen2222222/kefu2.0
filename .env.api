# 本地联调（PC）：前端 http://localhost:4000（Vite），经代理到后端（默认 4001；与 backend/.env 的 PORT 一致）
# 请先：cd backend && npm run dev（后端默认监听 4001，勿与前端同端口）
# 排查：npm run diag:dev（端口与健康检查）；npm run smoke:api（工单 API 冒烟，需后端已启动）
# 重要：若 4000 被占用，Vite 会启动失败（strictPort）；勿让前端与后端占同一端口。
#
# 【工单/售后为空？】只跑过 db:seed:production 时库里没有工单。需要演示数据请：cd backend && npm run db:seed
VITE_INTELLIDESK_DATA_SOURCE=api
VITE_API_BASE_URL=http://localhost:4000
# 若后端改端口，与 backend/.env 中 PORT 对齐，例如：VITE_API_PROXY_TARGET=http://127.0.0.1:4001
# VITE_API_PROXY_TARGET=http://127.0.0.1:4001
