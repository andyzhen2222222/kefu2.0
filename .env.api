# 本地联调：前端 http://localhost:4001，经 Vite 代理到后端（默认 4000；与 backend/.env 的 PORT 必须一致）
# 请先：cd backend && npm run dev（后端监听 4000，勿与前端同端口）
# 排查：npm run diag:dev（端口与健康检查）；npm run smoke:api（工单 API 冒烟，需后端已启动）
# 重要：若 4001 被占用，Vite 可能改用其他端口，请与浏览器地址栏一致；勿让 Vite 与后端同占 4000。
#        请保证仅一个 Vite 占 4001，且 4000 只跑 backend（可用 netstat -ano | findstr :4000 自查）。
#
# 【工单/售后为空？】只跑过 db:seed:production 时库里没有工单。需要演示数据请：cd backend && npm run db:seed
VITE_INTELLIDESK_DATA_SOURCE=api
VITE_API_BASE_URL=http://localhost:4001
# 若后端改端口，与 backend/.env 中 PORT 对齐，例如：VITE_API_PROXY_TARGET=http://127.0.0.1:4000
# VITE_API_PROXY_TARGET=http://127.0.0.1:4000
