# 本地联调：前端 http://localhost:4000，经 Vite 代理到后端 http://127.0.0.1:4001（请先 cd backend && npm run dev）
#
# 【收件箱/售后为空？】只跑过 db:seed:production 时库里没有工单。需要演示数据请：cd backend && npm run db:seed
VITE_INTELLIDESK_DATA_SOURCE=api
VITE_API_BASE_URL=http://localhost:4000
